{
  "name": "deltom",
  "private": true,
  "scripts": {
    "dev:mobile": "turbo dev --filter=mobile",
    "dev:web": "turbo dev --filter=web-guest",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
